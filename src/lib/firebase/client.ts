// MVP 1 — Firebase Client SDK is not connected yet.
// Replace this stub when adding real-time client-side subscriptions:
//
//   import { getApps, initializeApp } from "firebase/app";
//   import { getFirestore } from "firebase/firestore";
//   ... (see .env.example for NEXT_PUBLIC_FIREBASE_* setup)

export const db = null;
