// MVP 1 — Firebase Admin SDK is not connected yet.
// Replace this stub with real initialization when adding Firebase credentials:
//
//   import "server-only";
//   import { cert, getApps, initializeApp } from "firebase-admin/app";
//   import { getFirestore } from "firebase-admin/firestore";
//   ... (see .env.example for FIREBASE_ADMIN_BASE64 setup)

export const adminDb = null;
